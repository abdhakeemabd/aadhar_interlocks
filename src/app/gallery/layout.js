export const metadata = {
  title: "Project Gallery | Aadhar Interlocks",
  description: "View our completed interlocking paver projects and Instagram reels across Kerala.",
};

export default function GalleryLayout({ children }) {
  return <>{children}</>;
}
